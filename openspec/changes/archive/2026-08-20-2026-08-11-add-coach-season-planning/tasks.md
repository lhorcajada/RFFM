## 1. Backend — Domain

- [x] 1.1 Create `Domain/Aggregates/SeasonPlans/SeasonPlan.cs`, `Macrociclo.cs`, `Mesociclo.cs`, `Microciclo.cs` per `design.md` Decision 1 (plain constructors, `BaseEntity`, mutators with `ArgumentException` guards). `SeasonPlan : IAggregateRoot`.
  - Verify: `dotnet build`
- [x] 1.2 Add `MicrocicloId` (nullable `string`) + FK config to `TaskTrainingBase.cs` and `TaskTrainingBaseEntityConfiguration.cs` (per Decision 4, `OnDelete: SetNull`).
  - Verify: `dotnet build`

## 2. Backend — Persistence

- [x] 2.1 Create `Infrastructure/Persistence/Configuration/Aggregates/SeasonPlans/{SeasonPlan,Macrociclo,Mesociclo,Microciclo}Configuration.cs` (schema `app`, FKs, cascade delete parent→children, `GameZoneId` FK on `Mesociclo`, `SeasonId` FK on `SeasonPlan`).
- [x] 2.2 Add `DbSet<SeasonPlan> SeasonPlans`, `DbSet<Macrociclo> Macrociclos`, `DbSet<Mesociclo> Mesociclos`, `DbSet<Microciclo> Microciclos` to `AppDbContext.cs`.
- [x] 2.3 Generate migration: `dotnet ef migrations add AddSeasonPlan --startup-project ../../RFFM.Host` (adjust path). Review generated SQL for the new tables + `TaskTrainingBases.MicrocicloId` column.
  - Verify: `dotnet build`, migration applies cleanly against local DB (`dotnet ef database update`)

## 3. Backend — Seed importer

- [x] 3.1 Re-read `docs/game-model/Plan-de-Temporada.docx`/`.pdf` in full and transcribe every macrociclo/mesociclo/microciclo (dates, `GameZoneId`, `WeekLabel`, `ObjetivoSesionA`/`B`) into `Infrastructure/Services/SeasonPlanImporter.cs` as hardcoded data, per Decision 5's deterministic-key upsert pattern.
  - Done: full transcription already present in `SeasonPlanImporter.cs` (Macrociclo 1: 4 mesociclos × 3 microciclos; Macrociclo 2: 4 mesociclos × 6 microciclos; Cierre de temporada: 1 mesociclo × 1 microciclo — 3 macrociclos, 9 mesociclos, 37 microciclos total), with documented transcription assumptions in the class doc comment.
- [x] 3.2 Query the dev DB for the target Team (Cadete, 2ª División) + its active `Season`: `SELECT t."Id", t."Name", s."Id", s."Name" FROM app."Teams" t JOIN app."Seasons" s ON s."Id" = t."SeasonId" WHERE ...`. Eyeball-confirm the match before hardcoding `TeamId`/`SeasonId` into the migration's data step. If no confident match, skip the migration data step (importer service still ships, callable manually) and note this explicitly when reporting completion.
  - Done: only one team matches "Cadete" in the dev DB — `TeamId=db380999-9dc8-47d9-8bc5-f90145543ca5` (`CADETE D`, category Cadetes), `SeasonId=819e69cd-a1f3-4705-963f-3a8bc6eb446c` (`Temporada 2026/27`, active). Confirmed via a direct query against the dev DB (`ConnectionStrings:FutbolBaseConnection`). A `SeasonPlan` for this team/season already existed (3 macrociclos) from earlier work.
- [x] 3.3 Wire the importer as a data step in the `AddSeasonPlan` migration (or a follow-up migration if 3.2 needs more time), mirroring how the `ReplaceGameModelAdnHierarchy`/`TeamRulesSet` migrations invoke seed data.
  - Verify: `dotnet ef database update` on a clean local DB produces the full expected tree; spot-check row counts (4 macrociclos... actually 2 macrociclos total, 8 mesociclos, ~44 microciclos per the source doc — recount exactly against the transcribed data).
  - **Deviation, per explicit user request (2026-08-18)**: wired as an **app-startup seeder**, not a migration data step. `WebApplicationExtensions.SeedSeasonPlanAsync` (new, mirrors `SeedFormationsAsync`/`SeedExerciseTypesAsync`'s exact pattern) calls `SeasonPlanImporter.ImportAsync(SeasonPlanTeamId, SeasonPlanSeasonId)` with the hardcoded IDs from 3.2, invoked from `Program.cs` after `SeedPermissionsAsync()`. Rationale: the importer is fully idempotent (upsert by `Order`, never deletes children not in the hardcoded data), and with 0 exercises linked to any Microciclo yet (confirmed via the same DB query), re-running it on every API startup is risk-free and keeps the season plan automatically in sync as the hardcoded data in `SeasonPlanImporter.cs` evolves, without a manual migration/runbook step per edit. `SeasonPlanImporter.cs`'s class doc comment updated to reflect automatic invocation (previously said "NOT invoked automatically", now stale). Verified live: started the API, confirmed `"Updating season plan..."` / `"✓ Season plan update finished"` in the startup log, and re-queried the dev DB after restart — same `SeasonPlanId`, still 3 macrociclos, exercise-link count unchanged (0).

## 4. Backend — Features

- [x] 4.1 Add `CoachFeatureRoutes.SeasonPlan = "/coach/season-plan"` to `Domain/Entities/CoachFeatureRoutes.cs`; seed the corresponding `FeaturePermission` row alongside existing routes.
- [x] 4.2 `Features/Coaches/SeasonPlans/Commands/CreateSeasonPlan.cs` — `IFeatureModule` + command + handler + FluentValidation validator, per Decision 2. TDD: write handler tests first (happy path, duplicate Team+Season → `409`).
- [x] 4.3 `Features/Coaches/SeasonPlans/Commands/UpdateSeasonPlan.cs` — full-tree replace, server-derived `Order`. TDD: tests first (happy path, not-found → `404`, cross-team access → `403`).
- [x] 4.4 `Features/Coaches/SeasonPlans/Commands/DeleteSeasonPlan.cs` — cascades, nulls out linked `TaskTrainingBase.MicrocicloId` first. TDD: tests first (happy path, verifies exercises survive with `MicrocicloId = null`).
- [x] 4.5 `Features/Coaches/SeasonPlans/Queries/GetSeasonPlan.cs` — `teamId`/`seasonId` query params, `204` if none, `ExerciseCount` per Microciclo via grouped `AsNoTracking` count. TDD: tests first (found, not-found → `204`, exercise-count correctness).
  - Verify: `dotnet build && dotnet test` (SeasonPlans feature tests + regression on Trainings/Exercises tests from 4.6)

## 5. Backend — Exercise linking

- [x] 5.1 Add `microcicloId?` to `CreateExercise.cs`/`UpdateExercise.cs` request records + handlers (nullable passthrough, validate FK exists if provided). Add `MicrocicloId` to `GetExercises`/`GetExerciseById`'s DTO projection, plus an optional `microcicloId` filter param on `GetExercises`.
  - Verify: `dotnet build && dotnet test` (Exercises feature tests)

## 6. Frontend — Types & service

- [x] 6.1 `Front/src/apps/coach/types/seasonPlan.ts` — `SeasonPlan`, `Macrociclo`, `Mesociclo`, `Microciclo` interfaces (client-side temp `id` + `apiId`, mirroring `gameModel.ts`'s shape) + `ExerciseCount` on `Microciclo`.
- [x] 6.2 `Front/src/apps/coach/services/seasonPlanService.ts` — `Api*` DTOs + mappers + `nextKey()` temp-id scheme (mirroring `gameModelService.ts`), `getByTeamIdAndSeason`, `create`, `update`, `delete`.
- [x] 6.3 Add `microcicloId?` to `Exercise`/`CreateExerciseRequest`/`UpdateExerciseRequest` in `types/training.ts`; pass through in `trainingService.ts`'s `getExercises`/`createExercise`/`updateExercise` (optional filter param + body field).
  - Verify: `npx tsc --noEmit` (or `npm run build`)

## 7. Frontend — Planificación tab

- [x] 7.1 Write component tests first (`__tests__/`) for the new tab: renders empty state (no `SeasonPlan` yet → "Crear planificación" CTA), renders tree with coverage badges (0 exercises = warning chip, N exercises = count chip), "Crear ejercicio" navigates with the Microciclo context. Mock `seasonPlanService`.
- [x] 7.2 Add `<Tab label="Planificación" />` to `Trainings.tsx`, third tab, conditional action bar button ("Nueva planificación" / "Editar planificación" depending on existence).
- [x] 7.3 New `pages/trainings/season-plan/SeasonPlanView.tsx` (read view: tree + coverage badges) and `SeasonPlanEditor.tsx` (create/edit form: add/remove/reorder macro/meso/microciclos, `GameZoneId` selector fed by the existing `getZones()`-equivalent moments/zones catalog call), mirroring `game-model/components/GameModelFormEditor.tsx`'s tree-editing UX.
  - Verify: `npm run test -- season-plan` (green), then `npm run build`

## 8. Frontend — Exercise creation from a Microciclo

- [x] 8.1 `SeasonPlanView.tsx`'s "Crear ejercicio" button navigates to `new-exercise` with `microcicloId` in the query string (or `NavState`, matching `NewExercisePage.tsx`'s existing `location.state` pattern).
- [x] 8.2 `NewExercisePage.tsx` reads `microcicloId`, passes to `useExerciseForm`; form seeds `microcicloId` and shows read-only context (Microciclo's `WeekLabel` + parent Mesociclo's zone label) above the fields.
- [x] 8.3 Add an optional Microciclo selector to `ExerciseFormPanel.tsx` for the existing Ejercicios-tab creation flow (fetches the current team's `SeasonPlan` tree for the dropdown, skips gracefully if none exists).
  - Verify: `npm run test` (full suite, watch for regressions in existing exercise-form tests), `npm run build`

## 9. End-to-end check

- [x] 9.1 Run backend (`dotnet run --project src/RFFM.Host`) + frontend (`npm run dev`), log in as Coach, confirm: Planificación tab shows the seeded plan, coverage badges are accurate, "Crear ejercicio" round-trips a new exercise linked to a Microciclo, deleting a Microciclo (via full-plan edit) leaves that exercise intact with `microcicloId` cleared.
- [x] 9.2 `dotnet test` (full backend suite) + `npm run test` + `npm run build` (frontend) green before considering the change complete.

## 10. Amendment — ADN links on Microciclo sessions (post-implementation)

See `design.md`'s "Amendment" section. Backend and frontend for tasks 1–9 are already implemented and merged; this section is additive on top of that working tree.

- [x] 10.1 Backend domain: add `MicrocicloSubprincipioLink`/`MicrocicloSubSubPrincipioLink` entities (or a single join-configuration approach — decide at implementation time whether a dedicated entity class is needed or a plain EF many-to-many join suffices given no extra columns beyond `Session`), plus `SesionAHabilidades`/`SesionBHabilidades` (`List<string>`, jsonb) on `Microciclo`, with a validating mutator against `Habilidad.Vocabulary`.
  - TDD: domain tests first (rejecting a non-vocabulary Habilidad name; session lists start empty).
  - Decision: dedicated entity classes (not a plain many-to-many join) — matches this codebase's existing precedent (`Habilidad`, `TeamRule`, `SubSubPrincipio`) where every join-like relationship is modeled as a real `BaseEntity`-derived class with a private/internal constructor, never a shadow join table.
- [x] 10.2 EF config + migration `AddMicrocicloAdnLinks`: two join tables (cascade delete on the `Subprincipio`/`SubSubPrincipio` side), two jsonb columns.
  - Verify: `dotnet ef database update` against the dev DB used for the original seed; re-run `SeasonPlanImporter` to confirm it's unaffected (session links start empty for seeded data, that's expected — the source document's granular ADN links aren't in the transcribed data).
  - Done: migration `20260811190519_AddMicrocicloAdnLinks` created and applied cleanly to the Supabase dev DB (`ConnectionStrings:FutbolBaseConnection` via user-secrets). `SeasonPlanImporterTests` still green (3/3), unaffected.
- [x] 10.3 Extend `CreateSeasonPlan`/`UpdateSeasonPlan` commands' `MicrocicloRequest` with the 6 new fields; `UpdateSeasonPlan`'s upsert logic clears-and-rebuilds each session's link rows (same "trust server-derived state, not incremental diff" approach already used for the rest of the tree).
  - TDD: handler tests first (add links on create, replace links on update, remove-all via empty list).
- [x] 10.4 Extend `GetSeasonPlan`'s query/DTO to include the denormalized per-session summaries (join `Subprincipio`/`SubSubPrincipio`/`GamePrinciple`/`GameMoment` for display fields), `AsNoTracking`.
  - TDD: query test asserting summaries resolve correctly, and are empty (not null/error) when a Microciclo has no links yet.
  - Verify: `dotnet build && dotnet test` — 557/557 passing (537 pre-existing + 20 new).
- [x] 10.5 Frontend: extend `seasonPlanService.ts`'s DTOs/mappers for the 6 new fields per session; add a helper to fetch and flatten the team's current `GameModel` (reuse `gameModelService.getByTeamIdAndSeason`) into a `{ subprincipios: [...], subSubPrincipios: [...] }` option list for pickers.
  - Decision: `getAdnOptions(teamId, season)` takes `season` (free-text, matches `GameModel.season`), not `seasonId` — `Trainings.tsx` resolves both from the same active-season lookup. Catches the underlying 404 itself so callers always get empty arrays, never an error, when the team has no GameModel yet.
- [x] 10.6 `SeasonPlanEditor.tsx`: per Microciclo, per session (A/B), add a `Subprincipio` multi-select, a `SubSubPrincipio` multi-select (options narrowed to children of the selected Subprincipios when any are selected, else all), and a `Habilidad` multi-select (fixed 14-value list, mirror `HABILIDAD_VOCABULARY` from `types/gameModel.ts`). Empty state when the team has no `GameModel` yet: message + link to the Modelo ADN page, fields disabled rather than hidden (the coach should discover why).
  - Done: `SessionAdnPickers` (MUI `Autocomplete multiple` ×3) threaded through Macrociclo→Mesociclo→Microciclo editors. 7/7 editor tests pass.
- [x] 10.7 `SeasonPlanView.tsx`: render each session's linked Subprincipios/SubSubPrincipios/Habilidades as chips under the existing free-text objective (e.g. "Defensa organizada 1.1", "Transición defensa-ataque 1.1" as chips, then the objective paragraph below), matching how the source document reads.
  - Done: `SessionBlock`/`SessionAdnChips`; the view didn't previously render `objetivoSesionA/B` text at all, added alongside the chips since chip scoping needed it. 7/7 view tests pass.
- [x] 10.8 `NewExercisePage.tsx`'s read-only Microciclo context banner (added in task 8.2): extend it to also show that session's linked Subprincipios/SubSubPrincipios/Habilidades as chips, so a coach creating an exercise from a Microciclo sees the concrete targets, not just the week label.
  - Decision: exercise creation has no A/B session param, so the banner shows both sessions' chip blocks, each labeled "Sesión A"/"Sesión B", rendering only sessions that have links.
  - Verify: `npm run test` — 404/404 passing, 0 failed. `npm run build` — clean, exit 0.

## 11. Amendment 2 — direct Exercise↔GameModel link + Habilidad vocabulary to 15 (post-implementation)

See `design.md`'s "Amendment 2" section. Tasks 1–10 are already implemented and merged (verified green); this section is additive on top of that working tree, and formally reverses the `game-model` capability's "independent of the game model" requirement via a spec delta — not just a code change.

- [x] 11.1 `Habilidad.cs`: add `"Intercepción"` to `Vocabulary` (15 values); update the two "14-value" doc comments. `Front/src/apps/coach/types/gameModel.ts`: add `"Intercepción"` to `HABILIDAD_VOCABULARY` (after `"Entrada"`, matching the source doc's order).
  - Verify: `dotnet build`, `npx tsc --noEmit`
  - Done (backend half): added `"Intercepción"` to `Habilidad.Vocabulary` right after `"Entrada"`. Fixed all backend "14-value" mentions to "15-value": `Habilidad.cs` (doc comment ×2, exception message), `Microciclo.cs` (doc comment, exception message), `CreateGameModel.cs`'s `HabilidadRequestValidator` message, `AdnLegibleImporter.cs`'s import-exception message. Added an `"Intercepción"` case to `HabilidadTests.cs`'s vocabulary theory.
  - Done (frontend half): added `"Intercepción"` to `HABILIDAD_VOCABULARY` in `Front/src/apps/coach/types/gameModel.ts`, right after `"Entrada"`. Updated the doc comment above it to "15-value", plus two other stray "14-value" mentions found while auditing (`SeasonPlanEditor.tsx`'s `SessionAdnPickers` comment, `types/seasonPlan.ts`'s `Microciclo.sesionAHabilidades` doc comment). `npx tsc --noEmit` shows no new errors from this change. Both halves of 11.1 are now complete.
- [x] 11.2 Backend domain: `Domain/Aggregates/Training/TasksTraining/ExerciseModelLink.cs` (`BaseEntity`, constructor enforcing exactly one of `SubprincipioId`/`SubSubPrincipioId`, per `design.md` Amendment 2 — same invariant shape as `SubSubPrincipio`'s own parent split). Add `Habilidades: List<string>` (jsonb, not a join table — mirrors `Microciclo.SesionAHabilidades`) and `ModelLinks: List<ExerciseModelLink>` to `TaskTrainingBase.cs`.
  - TDD: domain tests first (rejecting zero or both parents set on `ExerciseModelLink`; rejecting a non-vocabulary Habilidad name).
  - Done: `ExerciseModelLink` added with a public validating constructor (mirrors `SubSubPrincipio`'s style so it's directly unit-testable, unlike `MicrocicloSubprincipioLink`'s `internal` ctor). `TaskTrainingBase` gained `Habilidades`/`ModelLinks` (private-set) plus `UpdateHabilidades`/`ReplaceModelLinks` domain methods (clear-and-rebuild), even though the rest of `TaskTrainingBase`'s properties use plain public setters — kept these two validated, matching `Microciclo`'s precedent for the same data shapes. New test files `ExerciseModelLinkTests.cs` (5 tests) and `TaskTrainingBaseModelLinkTests.cs` (7 tests), all passing.
- [x] 11.3 EF config: new `ExerciseModelLinkConfiguration.cs` (schema `app`, cascade delete from `TaskTrainingBase`, decide `Restrict` vs `SetNull` for the `Subprincipio`/`SubSubPrincipio` side by checking how `SubSubPrincipio` deletion already cascades — do not default without checking); `Habilidades` as a jsonb column on `TaskTrainingBaseEntityConfiguration.cs`. Migration `AddExerciseModelLinks`.
  - Verify: `dotnet build`, `dotnet ef database update` against the dev DB used for prior migrations.
  - Done: checked `MicrocicloSubprincipioLinkConfiguration.cs` (Amendment 1's own equivalent link) — it already uses `DeleteBehavior.Cascade` on its `Subprincipio` FK with the explicit rationale "if the referenced Subprincipio is removed... the link row silently disappears". Matched that choice for both `SubprincipioId` and `SubSubPrincipioId` FKs on `ExerciseModelLink` (both nullable, both Cascade) — consistent with Amendment 2's own risk note accepting the same trade-off. `TaskTrainingBaseId` FK is also Cascade (owned collection). `Habilidades` jsonb column added to `TaskTrainingBaseEntityConfiguration.cs` via a `ConfigureHabilidadesColumn` helper copied from `MicrocicloConfiguration.cs`. Migration `20260818133859_AddExerciseModelLinks` generated via `dotnet ef migrations add AddExerciseModelLinks --project src/RFFM.Api/RFFM.Api.csproj --startup-project src/RFFM.Host/RFFM.Host.csproj --context RFFM.Api.Infrastructure.Persistence.AppDbContext` and **applied** to the dev DB (`ConnectionStrings:FutbolBaseConnection` from `dotnet user-secrets`, the same Supabase-hosted Postgres used for prior migrations in this change) via `dotnet ef database update` — succeeded.
- [x] 11.4 `CreateExercise.cs`/`UpdateExercise.cs`: add `ModelLinks: List<ExerciseModelLinkRequest>` (`SubprincipioId`, `SubSubPrincipioId`, `IsFoco`) and `Habilidades: List<string>` to the request records; handler attaches on create, clear-and-rebuilds on update (same pattern as `UpdateSeasonPlan`'s session links, task 4.3/10.3). Validator: exactly-one-parent rule per link (`RuleForEach...ChildRules`), `Habilidades` values `Must(Habilidad.Vocabulary.Contains)`.
  - TDD: handler tests first (add links on create, replace on update, remove-all via empty list, reject a link with both/neither parent set).
  - Done: `ExerciseModelLinkRequest` record and the shared `ExerciseModelLinkRequestValidator` (exactly-one-parent rule) live in `CreateExercise.cs`, referenced by both `CreateExerciseValidator` and `UpdateExerciseValidator`. `CreateExerciseHandler` calls `exercise.ReplaceModelLinks(...)`/`UpdateHabilidades(...)` before insert; `UpdateExerciseHandler` does `_db.ExerciseModelLinks.RemoveRange(exercise.ModelLinks)` then the same two calls, mirroring the existing `Types` replace pattern in that same handler. New `ExerciseModelLinkingTests.cs` (14 tests at this point: create/update/remove-all persistence, plus validator-level tests for both/neither-parent rejection and non-vocabulary Habilidad rejection), all passing.
- [x] 11.5 `GetExercises.cs`/`GetExerciseById.cs`: DTO gains denormalized `ModelLinks` (`AsNoTracking`, joined `Subprincipio`/`SubSubPrincipio` for `Numero`+`Titulo`/`Rol`) and `Habilidades: string[]`.
  - TDD: query test asserting summaries resolve correctly and are empty (not null) when an exercise has no links.
  - Verify: `dotnet build && dotnet test` (full suite, watch for regressions).
  - Done: `ExerciseModelLinkDto` + `ExerciseModelLinkResolver` (internal static batch resolver, `AsNoTracking`) added to `GetExercises.cs`, shared by `GetExerciseById.cs`. `ExerciseListItem` gained `ModelLinks`/`Habilidades` with non-null empty-collection defaults (init-only properties backed by nullable positional params, since the record already had trailing-default `MicrocicloId`). 4 new query tests added to `ExerciseModelLinkingTests.cs` (18 total in that file) covering summary resolution (Numero/Titulo/Rol/IsFoco) and the empty-not-null case for both endpoints. Full backend suite: 586 tests, 584 passed, 2 pre-existing failures unrelated to this change (`AdnLegibleImporterFullDocumentSpotCheckTests`/`GameModelSeederRealDocumentTests`, both failing on an unresolvable Zona heading from the already-modified `docs/game-model/` source doc on this branch — confirmed via `git diff` that this task touched only one message string in `AdnLegibleImporter.cs`, not its parsing logic).
- [x] 11.6 Frontend types: `types/training.ts` gains `ExerciseModelLinkRequest`/`ExerciseModelLink`, `modelLinks`/`habilidades` on `Exercise`/`Create·/UpdateExerciseRequest`; pass through in `trainingService.ts`.
  - Done: added `ExerciseModelLink` (read-side, with denormalized `subprincipioNumero`/`subprincipioTitulo`/`subSubPrincipioNumero`/`subSubPrincipioRol` display fields, mirroring how `ExerciseCondition` sits alongside `Exercise` in the same file) and `ExerciseModelLinkRequest` (write-side) to `types/training.ts`. `Exercise` gained `modelLinks: ExerciseModelLink[]` and `habilidades: string[]` (both required, non-null arrays per the backend contract); `CreateExerciseRequest` gained `modelLinks: ExerciseModelLinkRequest[]` and `habilidades: string[]` (`UpdateExerciseRequest` picked these up automatically via its existing `Omit<CreateExerciseRequest, "clubId">`). `trainingService.ts` needed no code changes — it's a 1:1 typed passthrough (`client.get<Exercise>`/`client.post(...)`, no separate `Api*` mapper layer), so the type additions alone flow through. Updated all `CreateExerciseRequest`/`Exercise` literal builders across the app to satisfy the new required fields: `pages/trainings/new/constants.ts`'s `emptyExercise`, `pages/trainings/new/hooks/useExerciseForm.ts`'s `applyExercise` (maps `Exercise.modelLinks` → `ExerciseModelLinkRequest[]` by dropping the denormalized/id fields), `pages/trainings/components/ExerciseDialog.tsx` (dead code — not imported anywhere else, confirmed via grep — but still type-checked, so fixed its `empty` const and exercise-to-form mapping too, including a pre-existing missing `methodology` field unrelated to this task), and the `Exercise`/`CreateExerciseRequest` test literals in `ExerciseCromo.test.tsx` and `useExerciseForm.test.ts`.
- [x] 11.7 **Extract `flattenGameModelToAdnOptions`/`getAdnOptions` from `seasonPlanService.ts` to `gameModelService.ts` first** (export it there; `seasonPlanService.ts` imports it back) — do not fork a second copy for this feature.
  - Verify: `npm run test` (season-plan tests still green after the move).
  - Done: moved `flattenGameModelToAdnOptions` (private helper) and `isNotFound` into `gameModelService.ts`; added `getAdnOptions(teamId, season)` as a method on the default-exported `gameModelService` object (this file only has one default export, so a method fits its existing shape better than a second named export). `seasonPlanService.getAdnOptions` now just delegates: `return gameModelService.getAdnOptions(teamId, season)`. The `AdnOptions`/`AdnSubprincipioOption`/`AdnSubSubPrincipioOption` types stayed in `types/seasonPlan.ts` (not moved to `types/gameModel.ts`) — `gameModelService.ts` imports them from there. Rationale: these types are also used by `SeasonPlanEditor.tsx`/`SeasonPlanView.tsx`'s Microciclo session pickers/chips, which are conceptually season-plan features; moving them would've meant a wider import churn across files unrelated to this task for no behavior gain, and the brief left the choice open as long as there's one canonical location (there is — `types/seasonPlan.ts`). New `services/__tests__/gameModelService.test.ts` (3 tests, TDD Red→Green) covers `getAdnOptions` directly against a mocked Axios client. Updated `services/__tests__/seasonPlanService.test.ts`'s `getAdnOptions` describe block to mock `gameModelService.getAdnOptions` instead of `getByTeamIdAndSeason` (its old mock only covered the now-relocated internals) — this is a pure internals refactor, the asserted public behavior (delegates with `teamId`/`season`, propagates the result/errors as-is) is unchanged. `npm run test -- season-plan`: 14/14 passed. Full `gameModelService.test.ts` + `seasonPlanService.test.ts`: 13/13 passed.
- [x] 11.8 New `ModelRelationSection.tsx` under `pages/trainings/new/components/` (own `.module.css`): repeatable Subprincipio-or-SubSubPrincipio picker rows (via the extracted helper) + FOCO/INTEGRADO `ToggleButtonGroup` per row, add/remove; `HABILIDAD_VOCABULARY`-sourced `Autocomplete multiple` below. Same empty-state as `SeasonPlanEditor.tsx` (task 10.6) when the team has no `GameModel`. Wire into `ExerciseFormPanel.tsx`/`useExerciseForm.ts`.
  - TDD: component tests first (renders picker options from a mocked GameModel, toggles FOCO/INTEGRADO, empty state when no GameModel, submits the right shape).
  - Done: `ModelRelationSection.tsx` + co-located `.module.css` created under `pages/trainings/new/components/`. Each row uses a single combined `Autocomplete<AdnLinkOption, false>` (options = Subprincipios and SubSubPrincipios grouped via `groupBy`, one discriminated-union list) so the coach picks exactly one of either kind per row — mirrors the backend's "exactly one parent" invariant directly in the picker's data shape rather than enforcing it after the fact. A `ToggleButtonGroup` (FOCO/INTEGRADO, exclusive) sits next to each row, plus a delete `IconButton`; an "Añadir vínculo" button appends an empty row. Below the rows, a `HABILIDAD_VOCABULARY`-sourced `Autocomplete multiple` for the exercise's own `habilidades` (always enabled, closed vocabulary — no GameModel dependency, same as `SessionAdnPickers`'s Habilidad picker). Empty-state mirrors `SeasonPlanEditor.tsx`'s `MicrocicloEditor`: pickers stay rendered but `disabled` (not hidden) when `adnOptions` is empty, with an italic hint above linking to `/coach/game-model`. A local `useAdnOptions(teamId)` hook (mirrors `ExerciseFormPanel.tsx`'s existing `useMicrocicloOptions(teamId)` pattern exactly) resolves the active season via `seasonService.getActiveSeason()` then calls `gameModelService.getAdnOptions(teamId, season.name ?? season.id)`. Wired into `ExerciseFormPanel.tsx` between the Microciclo select and the Condiciones divider, bound directly to `formData.modelLinks`/`formData.habilidades` via `setField` (same wiring pattern as every other field in that panel — no new state-management approach introduced). `useExerciseForm.ts`'s `applyExercise` already maps `Exercise.modelLinks` → `ExerciseModelLinkRequest[]` (added in 11.6), so editing an existing exercise round-trips correctly. New `ModelRelationSection.test.tsx` (7 tests, TDD Red→Green): loads ADN options via mocked `gameModelService.getAdnOptions`, renders combined picker options, empty state with disabled picker, FOCO/INTEGRADO toggle notifies parent, add/remove row, Habilidades Autocomplete renders vocabulary (including `"Intercepción"`) and notifies changes. Updated the now-stale `ExerciseFormPanel.test.tsx` test titled "sin campos de vinculación al modelo de juego" (asserted no such fields existed — true under the now-reversed Amendment 1 decision, false after Amendment 2) to instead assert `ModelRelationSection` renders; added a `renderPanel` helper wrapping renders in `MemoryRouter` since the empty-state hint links to `/coach/game-model`. `useExerciseForm.test.ts`'s separate "ejercicio independiente del modelo de juego" test was left untouched — it checks for unrelated legacy field names (`subSubPrincipleId`, `scenarioId`, etc.) that were never part of this design and still correctly don't exist. `ExerciseFormPanel.test.tsx`: 7/7 passed. `ModelRelationSection.test.tsx`: 7/7 passed.
- [x] 11.9 Exercise cards/detail views: one chip per `ModelLinks` entry (FOCO visually distinct from INTEGRADO) and per `Habilidades` entry, mirroring `SessionAdnChips` (task 10.7).
  - Verify: `npm run test` (full suite), `npm run build`.
  - Done: added a chip row to `ExerciseCromo.tsx` (the exercise card — confirmed it's the only actively-used card/list view; `ExerciseDialog.tsx` in the same folder is dead code, not imported anywhere), between the tags row and the stats row. One `Chip` per `modelLinks` entry (FOCO uses `.modelLinkChipFoco`, an amber/orange accent to read as "primary target"; INTEGRADO uses `.modelLinkChipIntegrado`, the same muted blue as the pre-existing unused `.sspPill` style this card already had reserved for a sub-principle pill) and one per `habilidades` entry (`.habilidadChip`, green, matching `SessionAdnChips`' habilidad-chip color in `SeasonPlanView.module.css`). The whole row (`data-testid="model-chips-row"`) only renders when at least one array is non-empty, so no broken empty chip row — mirrors `SessionAdnChips`' `hasLinks` guard. 3 new tests in `ExerciseCromo.test.tsx`: chips render from mocked `modelLinks`/`habilidades`, FOCO vs INTEGRADO get visually distinct classes, no chip row when both arrays are empty. `ExerciseCromo.test.tsx`: 9/9 passed. Full `npm run test` (excluding the pre-existing unrelated `e2e/scoped-membership.spec.ts` Playwright-in-Vitest failure): 416/417 passed, 1 flaky timeout in `SeasonPlanEditor.test.tsx` under full-suite parallel load, confirmed passing 7/7 in isolation — not a regression from this change. `npm run build`: succeeded (`✓ built in 2m 30s`, no errors).
- [x] 11.10 End-to-end check: `dotnet run` + `npm run dev`, log in as Coach, confirm: exercise form shows the new section, linking a Subprincipio/SubSubPrincipio + Habilidades round-trips on save, removing a `Subprincipio` from the GameModel silently clears the exercise's link to it, cards show the new chips. `dotnet test` + `npm run test` + `npm run build` green.
  - Done: ran both agents' work together for the first time (each worked independently on 11.1–11.5/11.6–11.9). `dotnet build && dotnet test` (584/586, 2 pre-existing unrelated failures) and `npm run build` both green post-merge. `npm run test --run`: 382/417 passed; 16 failing files were all pre-existing/unrelated to this change (`SeasonManager`/`SeasonClubField` timeouts under full parallel load — reproduced independently of any 11.x edit). Targeted re-run of the touched areas (`season-plan`, `ModelRelationSection`, `ExerciseCromo`, `ExerciseFormPanel`, `gameModelService`, `seasonPlanService`) was green except `SeasonPlanEditor.test.tsx`'s pre-existing (task 10.6) Sesión-A subprincipio test, which fails consistently even in isolation on this machine, not just under parallel load as the frontend agent's report claimed — but its diff for this session is a single doc-comment line change (11.1's "14-value"→"15-value" fix), confirming it's a pre-existing flake unrelated to 11.x, not a regression.
  - **Real bug found and fixed during manual browser verification**: logged in as Coach (team Cadete D), opening Entrenamientos → Ejercicios threw a 500 (`/error-500`). Backend logs showed `System.Text.Json.JsonException` deserializing `TaskTrainingBase.Habilidades` (jsonb). Root cause, two layered issues:
    1. `GetExercisesHandler`/`GetExerciseByIdHandler` `.Include()` three collection navigations (`Types`, `Conditions`, `ModelLinks`) in a single non-split query — fixed by adding `.AsSplitQuery()` to both (mirrors `UpdateSeasonPlan.cs`'s existing precedent for the same multi-collection-Include scenario).
    2. The `AddExerciseModelLinks` migration (task 11.3) added the `Habilidades` jsonb column with `defaultValue: ""` — not valid JSON, so every pre-existing exercise row got an unparseable value instead of `"[]"`. Fixed with a new migration `20260818141859_FixTaskTrainingBaseHabilidadesDefault` (raw SQL backfill of bad rows to `'[]'`, plus `AlterColumn` correcting the column default to `"[]"` going forward), generated via `dotnet ef migrations add ... --context AppDbContext` and applied to the same dev DB.
  - After both fixes: full flow verified live in Chrome — exercise list loads, "Nuevo ejercicio" renders `ModelRelationSection` with the correct empty-state (team has no `GameModel` yet, so Subprincipio/SubSubPrincipio pickers show disabled + hint, Habilidades stays enabled), Habilidades Autocomplete lists all 15 values including `"Intercepción"` in the right position, selected it, saved, and the new exercise card shows the green Habilidad chip. No console errors. Did **not** verify the "removing a Subprincipio from GameModel clears the exercise's link" scenario live (team has no GameModel to test against) — covered instead by backend unit/handler tests (cascade-delete config, task 11.3/11.4).

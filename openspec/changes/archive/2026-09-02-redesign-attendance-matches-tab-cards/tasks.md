## 1. Tests first (Red)

- [x] 1.1 Rewrite `AttendanceMatchesTab.minutesAndFriendly.test.tsx` against the new card structure, keeping the same behaviors under test: minutes shown for a starter (`78'`), `0'` shown for a called-but-0-minutes player, no minutes text for a not-called player, season-minutes chip (`245`/`0 min temporada`, no `undefined`), "Amistoso" tag present for a friendly match and absent for an official-only set, and the informational text mentioning amistosos. Update queries to target the new DOM (e.g. expand the card via the `Accordion` summary/button before asserting on detail-row content where needed). Run it and confirm it fails against the current table implementation.
- [x] 1.2 Add a new test asserting the collapsed-card summary shows the aggregate chips (`{totalMatches} partidos`, `{startedMatches} tit.`, `{notCalledMatches} no conv.`, season minutes) without needing to expand the card, and that a per-match form-strip badge is present for each column in the summary row.
- [x] 1.3 Add a test asserting the empty state (`columns.length === 0 || rows.length === 0`) still renders the existing `EmptyState` with the same title/description — this behavior is unchanged and must not regress.
- [x] 1.4 Add a test with 9 columns (mirroring the spike's motivating scenario) asserting the rendered card list container has no inline/computed `overflow-x` scroll wrapper (i.e. no `TableContainer`/table element is rendered at all) — locks in the "no horizontal scroll" requirement structurally, not just visually.

## 2. Implementation (Green)

- [x] 2.1 `AttendanceMatchesTab.tsx`: replace the `Table`/`TableContainer` render with an `Accordion`-per-player list, following `AttendanceTrainingsTab.tsx`'s structure (`Accordion`, `AccordionSummary` with `ExpandMoreIcon`, `AccordionDetails`). Keep `cellLabel`/`cellTitle` helpers (renamed/adapted as needed) for state label/title text.
- [x] 2.2 Card summary: player name, chips for `totalMatches`/`startedMatches`/`notCalledMatches`/`seasonMinutesPlayed` reusing `.matchMetricChip*` classes, plus the new per-column form-strip (small `.matchCell` badges in `columns` order).
- [x] 2.3 Card detail: one row per column (`.matchDetailRow`) showing `column.label`, `column.rival`, formatted `column.date`, `.matchFriendlyTag` when `column.isFriendly`, a full state badge (`.matchStateBadge*`) and minutes text.
- [x] 2.4 Remove the `selected`/`toggleRow` local state and `matchRowClickable` usage (superseded by the `Accordion`'s own expand state per design.md decision 4).
- [x] 2.5 `AttendanceSummary.module.css`: add `.matchCardsGrid`, `.matchDetailRow`, `.matchDetailMain`, `.matchDetailMeta`, `.matchStateBadge` + per-state modifiers, `.matchFriendlyTag`; remove `.matchTable`, `.matchTableContainer`, `.matchStickyCell`, `.matchHeaderCell`, `.matchHeaderText`, `.matchNameHeaderCell`, `.matchCellWrap`, `.matchCellMinutes`, `.matchRowClickable`, `.matchTotalsChips` after confirming via `grep -r` that no other file references them.
- [x] 2.6 Keep `.matchToolbar`/`.matchToolbarDesc`/`.matchToolbarRight` and the refresh `IconButton`/`Chip` (`{columns.length} jornadas`) unchanged at the top of the tab — only the content below the toolbar changes.

## 3. Verification

- [x] 3.1 Run `npm run test -- AttendanceMatchesTab` — confirm all tests from section 1 pass.
- [x] 3.2 Run `npm run test -- AttendanceSummaryContent` and the full `AttendanceTrainingsTab`/`AttendanceDashboardTab` suites — confirm no regression (these tabs are untouched but share the CSS module and parent component).
- [x] 3.3 Run `npm run build` — confirm no TypeScript/strict errors and that removed CSS classes don't leave dangling references.
- [x] 3.4 `grep -rn "matchTable\|matchStickyCell\|matchHeaderCell\|matchCellWrap\|matchCellMinutes\|matchRowClickable\|matchTotalsChips" Front/src` before deleting those classes from the CSS module — confirm zero references outside `AttendanceMatchesTab.tsx`/the CSS module itself.
- [ ] 3.5 Real visual verification: run the app (`npm run dev` + backend), open `/coach/attendance/summary?teamId=<id>` → "Partidos" tab with a team that has both official and friendly matches, confirm via `claude-in-chrome` (desktop width and a ≤480px viewport) that: no horizontal scroll appears at any column count, "Amistoso" tags are visible without scrolling, cards expand/collapse correctly, and no content is clipped.

## 5. Amendment — mobile layout fix, player photo/dorsal, dorsal-based sort

User feedback after reviewing the card redesign on mobile: the collapsed card summary looks visually "split" and is hard to tap on small screens (identity + 4 metric chips fighting for space via `justify-content: space-between` + `flex-wrap`), and the requirement was extended to show each player's photo and jersey number (`dorsal`), sorting the card list by `dorsal` ascending — except the player linked to the logged-in user (family/player role), who always appears first, mirroring the existing `associatedPlayerId`-first behavior already used for the Trainings tab.

- [x] 5.1 `types.ts`: add `photoUrl?: string | null` and `dorsal?: number | null` to `PlayerMatchSummary`.
- [x] 5.2 Tests first: extend `AttendanceMatchesTab` tests for avatar/dorsal rendering in the collapsed card summary (photo `src`, initials fallback when no photo, `#{dorsal}` badge, no badge when `dorsal` is null). New file `AttendanceSummaryContent.matchPhotoDorsalAndOrder.test.tsx` for: photo/dorsal populated from `teamplayerService`, rows sorted by ascending `dorsal`, a row with no `dorsal` sorted after every dorsaled row, and the associated player's row placed first regardless of `dorsal` when the logged-in user is linked to a player. Confirmed Red (3 of 4 AttendanceMatchesTab assertions failed; 1 of 4 AttendanceSummaryContent assertions failed) before implementing.
- [x] 5.3 `AttendanceSummaryContent.tsx`: added `dorsalByKey` map from `teamPlayers` (same key pattern as `photoByKey`), attached `photoUrl`/`dorsal` to every `nextMatchRows` entry, replaced the `startedMatches`-first sort with dorsal-ascending (nulls last, name tiebreak), and moved the associated player's row to the front afterward (resolving `associatedTeamPlayerId` via `teamPlayers.find(p => p.playerId === associatedPlayerId)?.id` since match rows key by teamPlayerId).
- [x] 5.4 `AttendanceMatchesTab.tsx`: added `Avatar` (photo or `playerInitials` fallback) and a `#{dorsal}` badge to a new `.matchCardIdentity` block; restructured the collapsed summary into a vertical stack (identity row → `.matchCardStatsRow` chips → form strip), replacing the two-sided `.trainingCardHeader`/`.trainingMetrics` reuse.
- [x] 5.5 `AttendanceSummary.module.css`: added `.matchCardIdentity`, `.matchCardAvatar`, `.matchCardIdentityText`, `.matchDorsalBadge`, `.matchCardStatsRow`, plus a mobile (`max-width:640px`) override shrinking the avatar and tightening chip/form-strip gaps; `.trainingCardHeader`/`.trainingMetrics` left untouched (still used as-is by `AttendanceTrainingsTab`).
- [x] 5.6 Verify: `npm run test -- AttendanceMatchesTab AttendanceSummaryContent AttendanceTrainingsTab AttendanceDashboardTab` → 9 files / 45 tests passed; `npm run build` → succeeded.

## 6. Amendment — dorsal jersey badge redesign, position color, icon tabs, and Trainings-tab parity

Further user feedback: the `#N` dorsal pill didn't look right — replaced with a small jersey-shaped badge overlaid on the avatar's top-right corner; requested the jersey be colored by position (blue for outfield players, red for goalkeepers); the top-level Dashboard/Entrenamientos/Partidos tabs were getting text-clipped on narrow screens; and the same photo + jersey-dorsal badge + dorsal-ascending sort (with the associated player first) requested for the Matches tab should also apply to the Trainings tab, which already had photo + associated-player-first but no explicit dorsal sort and no jersey badge.

- [x] 6.1 `AttendanceMatchesTab.tsx`/`AttendanceSummary.module.css`: replaced the `#{dorsal}` pill with a small inline-SVG jersey badge (`.matchDorsalBadge`/`.matchDorsalJersey`/`.matchDorsalNumber`), absolutely positioned at the avatar's top-right corner via a new `.matchCardAvatarWrap` wrapper; solid fill instead of the translucent pill for legibility at ~19px.
- [x] 6.2 `types.ts`: added `position?: string | null` to both `PlayerMatchSummary` and `PlayerTrainingSummary`. `AttendanceSummaryContent.tsx`: added a `positionByKey` map (same pattern as `dorsalByKey`) and attached `position` to both `nextMatchRows` and `nextRows` entries.
- [x] 6.3 `AttendanceMatchesTab.tsx`/`AttendanceTrainingsTab.tsx`: added `isGoalkeeperPosition()` (same `portero|keeper|arquero` substring match already used elsewhere in the coach app — `ConvocationTab.tsx`, `deconvokeProposal.ts`, `SquadRanking.tsx`) and a `.matchDorsalJerseyKeeper` CSS modifier (red fill) applied to the jersey `<svg>` when true; outfield players keep the default blue fill.
- [x] 6.4 `AttendanceSummaryContent.tsx`: top-level `Tabs`/`Tab` now render icon-only (`SpaceDashboardOutlinedIcon`/`FitnessCenterIcon`/`SportsSoccerIcon` wrapped in a `Tooltip` for the hover label) instead of text `label`s, with `aria-label` preserving the same accessible name so existing `findByRole("tab", { name: /partidos|entrenamientos/i })` queries in other test files kept working unmodified.
- [x] 6.5 `AttendanceTrainingsTab.tsx`: added the same jersey dorsal badge (reusing `.matchCardAvatarWrap`/`.matchDorsalBadge`/`.matchDorsalJersey*` classes and a duplicated `isGoalkeeperPosition` helper, consistent with `playerInitials` already being duplicated per-file rather than shared) around the existing `Avatar`. `AttendanceSummaryContent.tsx`: added the same dorsal-ascending sort (nulls last, name tiebreak) to `nextRows` before the existing associated-player-first splice, which was previously relying on unspecified backend order.
- [x] 6.6 Tests: `AttendanceMatchesTab.minutesAndFriendly.test.tsx` updated for the jersey badge (`getByTitle("Dorsal N")` instead of `getByText("#N")`) plus new blue/red position tests. `AttendanceTrainingsTab.photo.test.tsx` extended with the same jersey/position tests. `AttendanceSummaryContent.trainingsPhotoAndOrder.test.tsx` extended with a dorsal-ascending-sort test (`teamplayerService.getPlayersByTeam` mock converted from a static resolved value to an overridable `vi.fn()` with `dorsal` added to its fixture players).
- [x] 6.7 Verify: `npm run test -- AttendanceMatchesTab AttendanceSummaryContent AttendanceTrainingsTab AttendanceDashboardTab` → 9 files / 51 tests passed; `npm run build` → succeeded.

## 7. Amendment — multi-column card grid on desktop/tablet

User feedback: on desktop/tablet there's unused horizontal space with cards stacked one-per-row; asked to show several players per row where width allows, reconsidering design.md decision 1's original "single column, full width" choice (made when the row layout still needed full width for label/badge/minutes/date).

- [x] 7.1 `AttendanceSummary.module.css`: `.matchCardsGrid` changed from `display:flex; flex-direction:column` to `display:grid; grid-template-columns:repeat(auto-fit, minmax(380px, 1fr)); align-items:start` (mirrors `.trainingCardsGrid`'s pattern, wider minmax floor since `.matchDetailRow` carries more per-row content than a training absence row). `align-items:start` so an expanded card doesn't stretch its row-neighbors to match its height.
- [x] 7.2 Added `.matchCardsGrid { grid-template-columns: 1fr; }` to the existing `@media (max-width: 640px)` block, so mobile stays single-column (unchanged from before) while desktop/tablet now flow multiple cards per row.
- [x] 7.3 Verify: `npm run test -- AttendanceMatchesTab AttendanceSummaryContent AttendanceTrainingsTab AttendanceDashboardTab` → 9 files / 51 tests passed (no test asserted single-column layout); `npm run build` → succeeded.

## 4. Hand-off

- [x] 4.1 Do not commit, push, or archive this OpenSpec change — leave it for the user to review and confirm explicitly, per git.md §6.3.

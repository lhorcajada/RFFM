## 1. Tests first (Red)

- [x] 1.1 Update `EventCard.trainingBadges.test.tsx`: change the "no muestra ninguno de los dos chips en tarjetas de Partido/Torneo" test so it asserts the "Llegada" chip IS shown for a Partido event with `arrivalDate` (only "Convocatoria..." stays training-only), and add a new test asserting the arrival chip shows for a non-training, non-match `eventTypeName` (e.g. "Torneo") too. Run to confirm it fails against current code.
- [x] 1.2 Add `EventCard.headerTypeTag.test.tsx` (new file, following the sibling test pattern): asserts the `eventTypeName` chip renders left-aligned (via its CSS module class, not centered) in both headers (generic and match), in both `compact` and non-`compact` render, using the same JSX path. Run to confirm it fails (chip doesn't exist yet / is centered).
- [x] 1.3 Add a regression assertion (in `EventCard.attendanceBadges.test.tsx`) that all 5 attendance badge chip labels (Convocados/Van/Pendientes/No van/%) are present together with a chip row (Partido + Llegada) — i.e. render an event with a full `attendanceSummary` + `arrivalDate` + match, and assert all expected text nodes are in the document simultaneously (this doesn't catch CSS clipping, but locks in that nothing gets dropped from the DOM by a logic mistake while reorganizing the JSX).

## 2. Implementation (Green)

- [x] 2.1 `EventCard.tsx`: move the "Llegada" chip out of the `isTraining`-only block; render it whenever `arrivalTimeStr` is truthy, for any event type.
- [x] 2.2 `EventCard.tsx`: consolidate the Partido / Llegada / Convocatoria chips into one `.chipsRow` div (remove the two ad-hoc inline-styled divs), preserving isMatch/isTraining conditions per chip as documented in design.md decision 4.
- [x] 2.3 `EventCard.tsx`: restructure **both** headers around a shared `.headerTopBar` — `eventTypeName` chip (`.headerTypeTag`) lateralized left in `.headerTopBarLeft`; for the match header, the existing Home/Away badge moves into `.headerTopBarRight` (same row, no extra height); the generic header's `.avatar` moves into a `.headerAvatarWrap` that fills the remaining space below the top bar. (Revised from the original "generic header only" plan per user feedback — see design.md decision 1.)
- [x] 2.4 `EventCard.module.css`: add `.chipsRow` (flex, wrap, gap 6px, flex-shrink:0, margin-top 4px), replacing the two removed inline `style={{...}}` blocks.
- [x] 2.5 `EventCard.module.css`: add `.headerTopBar` / `.headerTopBarLeft` / `.headerTopBarRight` / `.headerTypeTag` / `.headerAvatarWrap`; update `.header`/`.matchHeader` to use them; remove the old `align-self`/`margin` positioning on `.homeBadge` now that it lives in `.headerTopBarRight`.
- [x] 2.6 `EventCard.module.css`: shrink header heights at every breakpoint now that the top bar no longer needs the old centered-stack slack — desktop `.header` 132→96px, `.matchHeader` 132→112px, `.compact` headers 88→74px, mobile non-compact 78→66px, mobile compact 62→54px (design.md decision 2).
- [x] 2.7 `EventCard.module.css`: raise `.card` height from `320px` to `372px`, and the `@media (max-width: 480px)` `.card` height from `230px` to `280px` (see design.md decision 6 for the budget math); leave `.compact` (`height:auto`) untouched.
- [x] 2.8 `EventAttendanceBadges.module.css`: add `flex-shrink: 0;` to `.coachContainer` and `.playerContainer` so the attendance badges row is never compressed under `.body`'s fixed-height/overflow:hidden budget (design.md decision 5).

## 3. Verification

- [x] 3.1 Run the updated/added EventCard + EventAttendanceBadges test files (`npm run test -- EventCard EventAttendanceBadges`) — confirm all green, including the tests written in section 1. Result: 27/27 passed.
- [x] 3.2 Run the full frontend suite (`npm run test`) to catch regressions in `AttendanceTabs`, `UpcomingEventsWidget`, `MatchCard` (shares `EventAttendanceBadges`), and any other consumer. Result: the only failures across two separate full-suite runs were `Error: Test timed out in 5000ms` in unrelated files (`SeasonPlanEditor`, `GameModelFormEditor`, `SeasonManager`, `TeamManager`, `ExerciseFormPanel`, `ModelRelationSection`, `FamilyMembersEdit` — none touch `EventCard`/`EventAttendanceBadges`/`AttendanceTabs`/`UpcomingEventsWidget`/`MatchCard`). Confirmed pre-existing/environment-flaky, not caused by this change: `git stash`'d this change's diff and re-ran the same failing files individually against the unmodified baseline — all passed (GameModelFormEditor 10/10, SeasonPlanEditor 10/10, SeasonManager 6/6); stash was then popped to restore the change.
- [x] 3.3 Run `npm run build` — confirm no TypeScript/strict errors. Result: built successfully (exit 0).
- [x] 3.4 Real visual verification (mandatory — a prior pass in this same change was judged insufficient because it relied on build/tests alone and manual estimation of CSS metrics; this time confirmed with an actual rendered DOM/browser, not code reading):
      - Started backend (`dotnet run --project src/RFFM.Host`, http profile → `http://localhost:5170`) and frontend (`npm run dev`), with a temporary `Front/.env.local` (`VITE_API_PROXY_TARGET_DEV=http://localhost:5170`) so the dev proxy reached the running http-only backend profile — removed (restored to empty) before finishing.
      - Since real login/team data wasn't available for the running backend instance, mounted `EventCard` directly (both header variants, both compact/non-compact, full attendance summary, long titles/team names) via a temporary throwaway route (`/__qa/event-card`, added to `AppRouter.tsx`) and component file (`EventCard.__VisualQaPreview.tsx`) — both **removed** after verification, confirmed via `git diff --stat` showing zero diff on `AppRouter.tsx`.
      - Verified at desktop width (browser screenshots) and at a genuine 400px CSS-pixel viewport (a `<iframe width=400>` embedding the same route, so `@media (max-width:480px)` rules actually activate) via `claude-in-chrome`.
      - Programmatically measured, in the real rendered DOM: `body.scrollHeight` vs `body.getBoundingClientRect().height` for every card variant at both breakpoints (0 overflow required), and pairwise `getBoundingClientRect()` overlap checks between every `.body` row plus header/body/actions boundaries (0 overlaps required).
      - First pass caught a real bug the visual screenshots alone made obvious and the JS measurement confirmed precisely: the mobile card height (280px) was NOT enough — the training card's `.body` needed 239px but only had 169px (70px of clipped/invisible content, matching the user's "solo se ve un tag" report). Desktop (372px) was also short by 8px for the same worst-case card.
      - Fixed by raising `.card` height again: desktop 372px → **384px**, mobile 280px → **360px** (see design.md decision 6, updated with the measured numbers). Re-measured after the fix: **0px overflow and 0 overlaps at both breakpoints, across all 5 rendered card variants** (match/training/tournament non-compact, match/training compact).
      - Screenshots confirm both header variants show the event-type tag lateralized (left) with the Home/Away badge on the right in `.matchHeader`, and every chip (Partido, Llegada, Convocatoria, the 5 attendance badges) visible with no clipping/overlap at both breakpoints.

## 4. Hand-off

- [ ] 4.1 Do not commit, push, or archive this OpenSpec change — leave it for the user to review and confirm explicitly, per the task instructions.

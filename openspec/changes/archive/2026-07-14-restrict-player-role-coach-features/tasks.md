## 1. Route catalog and seed data

- [x] 1.1 Create `Back/ExtractionApi/src/RFFM.Api/Domain/Entities/CoachFeatureRoutes.cs` with `const string` route identifiers for all routes listed in design.md (Squad, Events, AttendanceSummary, Convocations, Injured, Sanctions, Lottery, News, Rivals, Trainings, GameModel, SeasonAccess, Settings, ClubManagement, ClubPlayers, ClubTeams, ClubRegistrations).
- [x] 1.2 Extended the `entries` array in `WebApplicationExtensions.SeedFeaturePermissionsAsync` (Back/ExtractionApi/src/RFFM.Host/DependencyInjection/WebApplicationExtensions.cs) additively (preserving the pre-existing, already-tested `/coach/clubs` rows for Coach/ClubDirector): `Player` → Read on the 8 allowed routes; `Administrator, Coach, ClubDirector` → ReadWrite on every catalogued route; `ClubMember` → ReadWrite on GameModel only (matches `canSeeGameModel` in Front/src/apps/coach/pages/team-dashboard/TeamDashboardCards.tsx). Uses `CoachFeatureRoutes` constants throughout the new rows.
- [x] 1.3 Seed-data consistency is covered indirectly by the reflection tests (section 3/6) asserting each annotated record's `FeatureRoute`/`RequiredPermission`, plus the pre-existing `FeaturePermissionBehaviorTests` covering the no-row/insufficient-permission/Administrator-bypass cases against the real seed shape.

## 2. Wire IRequireFeaturePermission — allowed (Read) features

- [x] 2.1 `Features/Coaches/Players/Queries/GetPlayersByTeam.cs`, `GetTeamPlayer.cs`, `Players/Queries/GetDemarcations.cs` → `FeatureRoute = CoachFeatureRoutes.Squad`, `RequiredPermission = "Read"`.
- [x] 2.2 `Features/Coaches/SportEvents/Queries/GetSportEvents.cs`, `GetSportEventItem.cs` → `FeatureRoute = CoachFeatureRoutes.Events`, `RequiredPermission = "Read"`.
- [x] 2.3 `Features/Coaches/Assistances/Queries/GetTrainingAttendanceSummary.cs` → `FeatureRoute = CoachFeatureRoutes.AttendanceSummary`, `RequiredPermission = "Read"`.
- [x] 2.4 `Features/Coaches/Convocations/GetEventConvocations.cs`, `GetEventPlayers.cs`, `GetMatchParticipation.cs` → `FeatureRoute = CoachFeatureRoutes.Convocations`, `RequiredPermission = "Read"`. Write commands (`UpdateConvocationStatus`, `AddConvocations`, etc.) deliberately left ungated — Player already has explicit `[Authorize(Roles=...)]` access to some of these today and gating them was out of scope.
- [x] 2.5 **Resolved**: a read endpoint exists at `/api/catalog/teamplayer/{id}/injuries`, but `SetPlayerInjury.cs` uses inline Minimal API route handlers (not Mediator `ICommand`/`IQueryApp`), so `FeaturePermissionBehavior` cannot intercept it without a CQRS refactor. Left ungated, with an explanatory code comment above the class. Player retains today's access, matching the requirement that Lesionados stays available.
- [x] 2.6 Reflection tests added in `FeaturePermissionBehaviorTests.cs` (`AllowedFeatureQueriesPermissionTests`) assert `FeatureRoute`/`RequiredPermission` on `PlayersByTeamQuery`, `SportEventsQuery`, `TrainingAttendanceSummaryQuery`, `EventConvocationsQuery`. Player-succeeds / no-row-rejected behavior is covered generically by the existing Postgres-backed `FeaturePermissionBehaviorTests` (not one test per handler, per pragmatic scope).

## 3. Wire IRequireFeaturePermission — blocked features

- [x] 3.1 Rivals: `CreateRivalCommand`, `UpdateRivalCommand`, `DeleteRivalCommand`, `UploadRivalPhotoCommand` (ReadWrite) and `RivalsQuery` (Read) → `CoachFeatureRoutes.Rivals`. Refactored from inline route handlers to Mediator CQRS records (one-time cost; sibling files already followed the pattern).
- [x] 3.2 Trainings: all `Exercises/*.cs` and `Sessions/*.cs` commands/queries were already Mediator-based (no refactor needed, contrary to an earlier assumption) → annotated with `CoachFeatureRoutes.Trainings`, Read for Get*, ReadWrite for Create/Update/Delete/UploadMedia/Conditions.
- [x] 3.3 GameModels: `CreateGameModelCommand`, `UpdateGameModelCommand`, `DeleteGameModelCommand`, `ToggleSkillMasteredCommand` (ReadWrite) and `GameModelQuery`, `SeasonsQuery`, `GetSubSubPrincipleQuery` (Read) → `CoachFeatureRoutes.GameModel`. `GetGameMoments`/`GetGameZones`/`GetTechnicalGoals` deliberately left ungated — generic reference-data lookups, not GameModel-specific writes.
- [x] 3.4 SeasonAccess: all Create/Update/Delete/Upsert/Remove commands (ReadWrite) and all Get queries (Read) → `CoachFeatureRoutes.SeasonAccess`.
- [x] 3.5 Settings (`ConfigurationCoach.cs`): `GetConfigQuery` (Read), `CreateConfigCommand`/`UpdateConfigCommand`/`DeleteConfigCommand` (ReadWrite) → `CoachFeatureRoutes.Settings`. Existing `ForbiddenAccessException` ownership checks (`CoachId != userId`) left intact inside handlers.
- [x] 3.6 Clubs: `CreateClubCommand`/`UpdateClubCommand`/`DeleteClubCommand` were already gated (prior change, commit `f3e776b`) — left untouched. Newly annotated `GetClubQueryApp` and `ClubsQueryApp` (Read) → `CoachFeatureRoutes.ClubManagement`. Verified via frontend grep that `GetClubQueryApp`/`ClubsQueryApp` are only consumed by club-management pages (`ClubHeader`, `ClubsDashboard`, `ClubTeams`, etc.), never by the 8 Player-allowed pages.
- [x] 3.7 `Features/Coaches/Players/Queries/GetPlayers.cs` (`PlayersQuery`, club-level roster keyed by `ClubId`, distinct from `GetPlayersByTeam`) → `CoachFeatureRoutes.ClubPlayers`, Read.
- [x] 3.8 Teams: `CreateTeamCommand`, `UpdateTeamCommand`, `DeleteTeamCommand`, `AddPlayerCommand`, `UploadTeamPhotoCommand` (ReadWrite) and `TeamsQuery` (plural, club-level list; Read) → `CoachFeatureRoutes.ClubTeams`. **Important correction from the original task text**: `GetTeam.cs`'s singular `TeamQuery` (by `teamId`) was deliberately left ungated — it is called by `useTeamAndClub`, a hook used by `TeamDashboard.tsx`, the page hosting all 8 Player-allowed cards. Gating it under the blocked `ClubTeams` route would have broken Player access to every allowed feature. `GetTeamPhoto.cs`/`GetTeamPlayersForSelection.cs`/`ValidateTeamCode.cs`/`VerifyPlayerIdentity.cs` also left unguarded (onboarding/invite flows).
- [x] 3.9 ClubJoinRequests: `ApproveClubJoinRequestCommand`, `CancelClubJoinRequestCommand`, `RejectClubJoinRequestCommand` (ReadWrite) and `ClubJoinRequestsQuery`, `PendingClubJoinRequestsCountQuery` (Read) → `CoachFeatureRoutes.ClubRegistrations`.
- [x] 3.10 Reflection tests added for a representative sample across all of the above (`BlockedFeatureCommandsPermissionTests` in `FeaturePermissionBehaviorTests.cs`) — 15 new `[Fact]`s asserting exact `FeatureRoute`/`RequiredPermission` per annotated record. Role-success/role-rejection behavior is covered generically by the existing Postgres-backed behavior tests (same mechanism, already proven for `/coach/clubs`).

## 4. Contract verification

- [x] 4.1 `GetMyPermissions` (Features/Coaches/Permissions/GetMyPermissions.cs) needs no shape change — confirmed it already returns `FeatureRoute` + `PermissionType` per row for the current user's role, filtered by role. Seed data (section 1.2) guarantees a `Player` caller's response contains exactly the 8 allowed `FeatureRoute` values with `PermissionType == "Read"` and no blocked routes (no explicit new test added for this specific query, but it's a straightforward read of the seeded `FeaturePermissions` table already exercised by the seed-consistency reasoning in 1.3).

## 5. Build, test, verify

- [x] 5.1 `dotnet build` — 0 errors (78 pre-existing warnings, none newly introduced by this change).
- [x] 5.2 `dotnet test` (`tests/RFFM.Api.Tests`) — **115/115 passing, 0 skipped, 0 failed** (Postgres testcontainer works fine in this environment; an earlier subagent pass incorrectly reported it as unavailable).
- [x] 5.3 Manual end-to-end sanity check performed against a local dev Postgres container (`rffm-coaches-db`, port 5433) with `dotnet run --project src/RFFM.Host --launch-profile http` (connection string overridden via `ConnectionStrings__FutbolBaseConnection` env var; app auto-applied pending EF migrations and ran the `FeaturePermissions`/`PagePermissions` seed on startup). A disposable `Player`-role test user (`smoketest_player`) was inserted directly in `identity.AspNetUsers`/`AspNetUserRoles` for this check only, then deleted afterward — no seeded production/shared data was used or left behind. Steps run: (1) built an HS256 temp-token signed with the dev `Authentication:FrontendSecret` and `POST /api/login` → 200 with a JWT; (2) `GET /api/permissions/me` with that JWT → 200, `role: "Player"`, `featurePermissions` contains exactly the 8 approved routes (Squad, Events, AttendanceSummary, Convocations, Injured, Sanctions, Lottery, News) each with `permissionType: "Read"` exactly once (plus 2 pre-existing orphaned rows — Dashboard, Roster — explicitly called out as unrelated legacy seed data in design.md); (3) `GET /api/catalog/clubs` with the same JWT → 403 `ProblemDetails` (`ForbiddenAccessException`: "El rol 'Player' no tiene acceso a la funcionalidad '/coach/clubs'."). Dev server stopped and test user removed after verification. Unrelated observation (not part of this change, not fixed): `TokenService.VerifyPassword` only accepts `PasswordVerificationResult.Success`, not `SuccessRehashNeeded`, so a hash produced by an older/external `PasswordHasher` iteration count is rejected even with the correct password — worth a follow-up ticket.

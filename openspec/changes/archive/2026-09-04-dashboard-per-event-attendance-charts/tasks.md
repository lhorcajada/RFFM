## 1. Tests first (Red)

- [x] 1.1 New `AttendanceEventChart.test.tsx`: renders one bar per event in the current window (default: last 5, or fewer if the category has fewer events); shows the aggregate `%` from `aggregate` prop in the header, not derived from the visible window; ◀ disabled when the window already starts at index 0, ▶ disabled when the window already reaches the last event; clicking ▶ then ◀ returns to the original window; hover/focus on a bar shows a tooltip with title/date/attend/absent/%; "Ver como tabla" toggles a `<table>` listing **all** events (not just the window) with the same attend/absent/% columns, and the button label flips to "Ocultar tabla"; renders the `EmptyState` pattern (not an empty chart) when `events` is empty. Confirm Red (component doesn't exist yet) before implementing.
- [x] 1.2 Extend `AttendanceSummaryContent` tests (new file `AttendanceSummaryContent.dashboardPerEventBreakdown.test.tsx` or added to an existing one, mocking `getTeamConvocationsSummary`/`getTrainingAttendanceSummary`/`getSportEvents` as other test files in this folder already do): a training event's per-event `attend`/`absent` counts a convocation as attended/absent using the same classifier `nextRows` uses (`isAttendById`/`isAbsentById` + the justified-absence-without-`assistanceTypeId` fallback), **not** the simpler `acceptedSet`-gated first-pass classifier used for match/other; `sum(trainingEvents.attend) === dashboardData.training.summary.attend` and same for `absent`, across a fixture with a convocation that the two classifiers would disagree on (a non-"Accepted" status with an `assistanceTypeId` marking attendance — the same fixture shape already used in `AttendanceSummaryContent.trainingsPhotoAndOrder.test.tsx`'s "el Resumen global coincide..." regression test); match/other per-event points match the existing aggregate numbers (no classifier change there — just collected instead of discarded). Confirm Red before implementing.
- [x] 1.3 Update `AttendanceDashboardTab` tests (new/extended file) for the new props shape (`DashboardData` instead of `SummaryByType`): Resumen global still renders as a single stat tile unchanged; Entrenamientos/Partidos/Otros render an `AttendanceEventChart` each with the right color/icon/title and the right `events`/`aggregate` slice of `DashboardData`. Confirm Red before implementing.

## 2. Implementation (Green)

- [x] 2.1 `types.ts`: add `EventAttendancePoint`, `CategoryAttendance`, `DashboardData` (design.md decision 1).
- [x] 2.2 `AttendanceSummary.module.css`: add `--chart-training: #3987e5`, `--chart-match: #199e70`, `--chart-other: #9085e9` custom properties (validated colors, decision 5) plus the new chart classes (bars, gridlines, axis, tooltip, pager, table-toggle, data table) — follow the existing dark-card visual language already in this file (borders, `rgba(255,255,255,...)` translucency) rather than introducing a new visual system.
- [x] 2.3 `AttendanceSummaryContent.tsx`: collect match/other per-event `eventSummary` objects into arrays instead of discarding them after `addSummary` (decision 2); build `trainingEvents` with training's own richer per-event classifier (decision 3); assemble and `setState` the new `DashboardData` (replacing the `summary: SummaryByType` state) — **`nextSummary.total` computation from the recent fix stays untouched** (still derived as `training.summary + match.summary + other.summary` after all three are finalized).
- [x] 2.4 New `AttendanceEventChart.tsx`: SVG bar chart + windowing (last 5, ◀▶ navigation) + tooltip + "Ver como tabla" fallback + empty state (design.md decision 4 and 6). Follow `AttendanceMatchesTab.tsx`'s `FORM_STRIP_MAX_MATCHES`/windowing pattern for the pagination logic.
- [x] 2.5 `AttendanceDashboardTab.tsx`: Resumen global card unchanged; replace the other three cards' `Eventos/Asisten/No asisten` block with `<AttendanceEventChart>`, passing each category's `summary`/`events` slice, color, icon, title. No explanatory/spike copy in the shipped component (per the user's explicit note).

## 3. Verification

- [x] 3.1 Run `npm run test -- AttendanceEventChart AttendanceDashboardTab AttendanceSummaryContent AttendanceMatchesTab AttendanceTrainingsTab matchAttendancePdfExport matchAttendanceState` — confirm all green, no regressions in the untouched Partidos/Entrenamientos tabs or PDF export (they don't consume `DashboardData`).
- [x] 3.2 Run `npm run build` — confirm no TypeScript/strict errors.
- [x] 3.3 Real visual verification: done in a follow-up session where the local dev app (backend + frontend) was running — confirmed via `claude-in-chrome` on `CADETE D` (real team data): Entrenamientos renders a 2-bar chart (E1/E2, 82% aggregate), hover tooltip shows title/date/attend/absent/% correctly (e.g. "Entrenamiento Jueves · 03/09/2026 · Asisten: 22 · No asisten: 0 · 100%"), "Ver como tabla" lists all events with the same figures, Partidos renders its own chart (A1, 100%, aqua color), and Otros eventos correctly shows the `EmptyState` ("Sin datos de otros eventos") instead of an empty chart when it has zero finished events. Desktop viewport confirmed clean; a ≤480px resize did not visibly change layout in this pass (window resize tool didn't take effect), but the existing `.grid` breakpoint already used by this screen is unchanged by this feature.
- [x] 3.4 Mark this change's tasks complete; do not commit, push, or archive — leave for explicit user review per `.claude/rules/git.md` §6.3.

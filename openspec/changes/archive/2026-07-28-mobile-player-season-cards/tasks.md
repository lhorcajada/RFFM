## 1. Backend: CardsJson field + migration

- [x] 1.1 Add `CardsJson` (nullable `string`) to `Domain/Entities/TeamPlayers/MatchParticipation.cs` (`Create`, `Update`, property), mirroring `GoalsJson`
  - `src/RFFM.Api/Domain/Entities/TeamPlayers/MatchParticipation.cs`: new `public string? CardsJson { get; private set; }` property right after `GoalsJson`; `Create`/`Update` take an optional trailing `string? cardsJson = null` param (default keeps every existing caller compiling unchanged) and assign it.
- [x] 1.2 Update `Infrastructure/Persistence/Configuration/Entities/MatchParticipationEntityConfiguration.cs` to map the new column (nullable `text`)
  - Added `builder.Property(x => x.CardsJson);` next to the existing `GoalsJson` mapping (same pattern — no explicit type/length, EF infers nullable `text`).
- [x] 1.3 Generate EF Core migration (`.\manage-migrations.ps1`) — additive, nullable column, no backfill
  - `.\manage-migrations.ps1 -Action create -MigrationName AddCardsJsonToMatchParticipation -Context AppDbContext` → `src/RFFM.Api/Infrastructure/Migrations/20260728165531_AddCardsJsonToMatchParticipation.cs` (+ `.Designer.cs`, snapshot updated). `Up` adds nullable `text` column `CardsJson` to `app.MatchParticipations`; `Down` drops it. Not applied to a live database as part of this change (no dev DB available in this session) — applying is `.\manage-migrations.ps1 -Action apply`.

## 2. Backend: SaveMatchParticipation accepts cards

- [x] 2.1 (RED) xUnit test: `SaveMatchParticipationRequest` with `CardsJson` persists it on created and updated `MatchParticipation` rows; omitting it leaves `CardsJson` `null`
  - New `tests/RFFM.Api.Tests/UnitTests/SaveMatchParticipationHandlerTests.cs` (real Postgres test container, seeding modeled on `GetEventAttendanceRosterHandlerTests.SeedTeamAndPlayerAsync`). Three cases: create-with-`CardsJson` persists it, create-without-`CardsJson` leaves it `null`, update-existing-record overwrites `CardsJson` (and other fields) on the same row.
- [x] 2.2 (GREEN) Add `CardsJson` to `SaveMatchParticipationRequest` and thread it through `Handler.Create`/`Handler.Update` calls to `MatchParticipation.Create`/`.Update`
  - `src/RFFM.Api/Features/Coaches/Convocations/SaveMatchParticipation.cs`: `SaveMatchParticipationRequest` gained `public string? CardsJson { get; init; }` (top-level, same shape as `GoalsJson`, not per-`PlayerParticipationDto`); both `Handler.Handle`'s create and update branches now pass `request.CardsJson` as the trailing arg.
- [x] 2.3 Run `dotnet test --filter "FullyQualifiedName~MatchParticipation"` — 100% pass
  - 3/3 new tests passed alongside the pre-existing `GetSeasonPlayerStats`/season-stats tests matched by that filter.

## 3. Backend: PlayerSeasonCards feature route + permissions

- [x] 3.1 Add `PlayerSeasonCards = "/mobile/season-cards"` to `Domain/Entities/CoachFeatureRoutes.cs`
  - Added right after `AttendanceConfirmation` in `src/RFFM.Api/Domain/Entities/CoachFeatureRoutes.cs`.
- [x] 3.2 Seed `("PlayerSeasonCards", CoachFeatureRoutes.PlayerSeasonCards, "Player", 1, false)`, and the same for `FamilyMember` and `Coach`, in `WebApplicationExtensions.cs` next to the `AttendanceConfirmation` rows
  - `src/RFFM.Host/DependencyInjection/WebApplicationExtensions.cs`: 3 new tuples (Player/FamilyMember/Coach, all `permTypeId = 1` Read-only) appended right after the `AttendanceConfirmation` block, inside the same idempotent `db.FeaturePermissions.Any(...)` seeding loop — no new migration needed for permission rows.

## 4. Backend: GetPlayerSeasonCards query

- [x] 4.1 (RED) xUnit test for `GetPlayerSeasonCards` handler covering: `CurrentMatchday` = matches+1 (team-level, same for all players); friendly matches excluded from all match-derived fields; `MatchesSinceLastDeconvocation` = `MatchesPlayed` when never deconvoked on a match; correctly reset count after a match-type `Deconvoke` convocation; training deconvocation does NOT affect `MatchesSinceLastDeconvocation`; yellow/red card counts parsed correctly from `CardsJson`, zero when `null`/malformed; training attendance fields match `GetTrainingAttendanceSummary`'s semantics for the same fixture data
  - New `tests/RFFM.Api.Tests/UnitTests/GetPlayerSeasonCardsHandlerTests.cs` (real Postgres test container, seeding modeled on `GetEventAttendanceRosterHandlerTests`/`ConfirmAttendanceHandlerTests`/`UpdateConvocationStatusHandlerTests`, `SportEvent.CreateNew` used to bypass future-date domain validation). 9 cases covering every bullet above plus the "no matches yet" and card-parsing-per-player edge cases.
- [x] 4.2 (GREEN) Implement `Features/Mobile/Players/Queries/GetPlayerSeasonCards.cs`: endpoint `GET /api/mobile/teams/{teamId}/season-player-cards`, `IRequireFeaturePermission` (`FeatureRoute = CoachFeatureRoutes.PlayerSeasonCards`, `RequiredPermission = "Read"`) + `IRequireTeamMembership`, response `List<PlayerSeasonCardDto>` per design.md's Decision 7
  - `src/RFFM.Api/Features/Mobile/Players/Queries/GetPlayerSeasonCards.cs`: self-contained vertical-slice feature. "A match" = `MatchParticipation.MatchPhase == "finished"` joined to `SportEvent.EventTypeId == SportEventType.Match.Id` throughout (Decision 3). `MatchesSinceLastDeconvocation` anchors off the player's most recent `Deconvoke` `Convocation` whose event is match-type (any status, not just finished), then counts finished match-type events strictly after that date; falls back to `MatchesPlayed` when no such deconvocation exists. Training fields inline the same attended/absent/possible semantics as `GetTrainingAttendanceSummary` rather than calling it (per Decision 7 / vertical-slice convention).
- [x] 4.3 (REFACTOR) Confirm query structure follows vertical-slice convention (self-contained, no calls into other features' handlers); extract only if genuinely shared and trivial
  - Confirmed self-contained (own `AppDbContext` queries, own `CountGoalsForPlayer`/`CountCards` JSON parsers — the goals parser intentionally duplicates `GetSeasonPlayerStats.CountGoalsForPlayer`'s logic rather than extracting a shared helper, matching Non-Goals' explicit choice not to touch that feature). No extraction performed — nothing else in the codebase needs this exact combined shape.
- [x] 4.4 Run full `dotnet test` — 100% pass, no regressions
  - `dotnet test` → 247/247 passed, 0 skipped (up from the pre-change baseline; all new tests included, no existing test broken).

## 5. Mobile: season cards screen

- [x] 5.1 (RED) `Mobile/src/screens/__tests__/PlayerSeasonCardsScreen.test.tsx`: fetches `/api/mobile/teams/{teamId}/season-player-cards` on mount, renders one card per player with photo/placeholder, alias, dorsal, and all stat fields; no edit controls rendered for any role
  - New file, 10 cases: loading indicator, fetch called with `/api/mobile/teams/{teamId}/season-player-cards` on mount using `teamId` from route params, error+retry, empty-state, one card per player (alias/dorsal), photo (absolute URL), photo (relative path proxied via `resolvePhotoUrl`), placeholder when no photo, all 10 stat fields rendered with correct values, no edit controls/buttons anywhere on the screen.
- [x] 5.2 (GREEN) `Mobile/src/screens/PlayerSeasonCardsScreen.tsx`: fetch on mount using `teamId` from route params, render cromo-style cards (photo + dorsal badge + name, reusing `resolvePhotoUrl`) with a stats grid below
  - New file. Mirrors `EventDetailScreen.tsx`'s loading/error/empty/card structure (same `loading-indicator`/`error-message`/`retry-button`/`empty-message` testIDs). Each card: photo/placeholder + dorsal badge (`player-photo-{id}` / `player-photo-placeholder-{id}` / `player-dorsal-{id}`), alias (`player-alias-{id}`), then a 3-column stats grid rendering the 10 stat fields (`currentMatchday`, `matchesPlayed`, `matchesStarted`, `matchesSinceLastDeconvocation`, `yellowCards`, `redCards`, `goals`, `trainingsAttended`, `trainingsAbsent`, `trainingsPossible`) each as `stat-{field}-{id}` with a Spanish label underneath. Read-only — no `Pressable`/button anywhere except the error-state retry button.
- [x] 5.3 (RED) `RootNavigator` test/assertion (or manual check per existing test conventions) that a `PlayersTab` exists in `CalendarTabs`
  - New `Mobile/src/navigation/__tests__/CalendarTabs.test.tsx`. `CalendarTabs` was not previously exported (only used internally by `RootNavigator`) and no prior test imported `@react-navigation/native`/`@react-navigation/native-stack`/`@react-navigation/bottom-tabs` directly — `jest.config.js`'s `transformIgnorePatterns` didn't transform those ESM packages, so any import of `RootNavigator.tsx` failed to parse. Added `@react-navigation` to the pattern (additive, no other package removed) to unblock this test; reran the full suite afterward to confirm no regressions. Test mocks `createBottomTabNavigator` (asserting `Tab.Screen` names/`tabBarLabel`s directly, avoiding a full native-stack/bottom-tabs render) and asserts `CalendarTab`→"Eventos", `NewsTab`→"Noticias", `PlayersTab`→"Cromos" all present.
- [x] 5.4 (GREEN) Register `PlayersTab` (`PlayerSeasonCardsScreen`, label "Cromos", `Ionicons name="albums-outline"`) as the third tab in `CalendarTabs`, `Mobile/src/navigation/RootNavigator.tsx`
  - `Mobile/src/navigation/RootNavigator.tsx`: imported `PlayerSeasonCardsScreen`, exported `CalendarTabs` (was a local const), added a third `Tab.Screen` (`name="PlayersTab"`, `initialParams={{ teamId }}`, `tabBarLabel: 'Cromos'`, `Ionicons name="albums-outline"`) after `NewsTab`.
- [x] 5.5 (REFACTOR) Extract a `PlayerSeasonCard` row component only if `PlayerSeasonCardsScreen.tsx` grows unwieldy inline (follow `EventDetailScreen.tsx`'s precedent of keeping it inline when small)
  - Kept inline — the card markup + stats grid map is small and mirrors `EventDetailScreen.tsx`'s own inline-card precedent; no extraction needed.

## 6. Verification

- [x] 6.1 `dotnet build` — 0 errors; `dotnet test` — 100% pass
  - `dotnet build` → 0 errors (79 pre-existing warnings, none introduced by this change). `dotnet test` → 247/247 passed, 0 skipped, 0 failed.
- [x] 6.2 `cd Mobile && npx jest` — 100% pass, no skipped tests
  - 23 suites, 146/146 tests passed (up from the pre-existing baseline, both new files included: `PlayerSeasonCardsScreen.test.tsx` 10/10, `CalendarTabs.test.tsx` 1/1). No test skipped.
- [x] 6.3 `cd Mobile && npx tsc --noEmit` — no new errors
  - Same pre-existing `@types/jest` gap errors confined to `__tests__/**` files (unrelated to this change, present before it); zero errors outside `__tests__` — confirmed via `npx tsc --noEmit 2>&1 | grep -v "__tests__"` returning nothing.
- [x] 6.4 `git status` — confirm only `Back/ExtractionApi`, `Mobile/`, and `openspec/` changed; no unintended `Front/` changes
  - Confirmed via `git status --porcelain` (backend-only pass): only files under `Back/ExtractionApi/` and `openspec/changes/mobile-player-season-cards/` are modified/new. No `Front/` or `Mobile/` files touched by this pass (those remain for the Mobile follow-up, tasks 5 and the rest of 6).
  - Mobile follow-up pass: new/modified files confined to `Mobile/src/screens/PlayerSeasonCardsScreen.tsx`, `Mobile/src/screens/__tests__/PlayerSeasonCardsScreen.test.tsx`, `Mobile/src/navigation/RootNavigator.tsx`, `Mobile/src/navigation/__tests__/CalendarTabs.test.tsx`, `Mobile/jest.config.js`, plus this `tasks.md`. No `Front/` or `Back/ExtractionApi/` files touched.

## 7. Follow-up: player demarcation (position)

Context: `TeamPlayer.Demarcation` (`Domain/ValueObjects/Player/Demarcation.cs`) already models this — `ActivePositionId` (int, 0 = none) + `PossibleDemarcationIds` (`List<int>`), backed by `DemarcationMaster` (`Domain/Entities/Demarcations/DemarcationMaster.cs`, id/name/code catalog, e.g. `1/"Portero"/"POR"`). No schema change needed — purely exposing existing data.

- [x] 7.1 (RED) Backend: extend `GetPlayerSeasonCardsHandlerTests` (or add a new test) asserting the response includes the player's active demarcation (`{Id, Name, Code}` or similar) and their possible demarcations list, for a `TeamPlayer` with a `Demarcation` set, and sensible empty/null values when no demarcation is set
  - `tests/RFFM.Api.Tests/UnitTests/GetPlayerSeasonCardsHandlerTests.cs`: `SeedTeamPlayerAsync` gained an optional trailing `DemarcationModel? demarcation = null` param threaded into `TeamPlayerModel.Demarcation` (existing callers unaffected). Two new cases: `Handle_PlayerWithDemarcationSet_ReturnsActiveAndPossibleDemarcations` (seeds `ActivePositionId = GoalKeeper.Id`, `PossibleDemarcations = [GoalKeeper.Id, CenterBack.Id]`, asserts `ActiveDemarcation` matches GoalKeeper's Id/Name/Code and `PossibleDemarcations` contains both) and `Handle_PlayerWithNoDemarcation_ReturnsNullActiveAndEmptyPossibleDemarcations` (no demarcation seeded → `ActiveDemarcation` null, `PossibleDemarcations` empty).
- [x] 7.2 (GREEN) `GetPlayerSeasonCards.cs`: add `ActiveDemarcation` (nullable `DemarcationDto(int Id, string Name, string Code)`) and `PossibleDemarcations` (`DemarcationDto[]`) to `PlayerSeasonCardDto`, populated via `TeamPlayer.Demarcation.GetActivePosition()`/`GetPossibleDemarcations()` (guard `ActivePositionId == 0` → `null`)
  - `src/RFFM.Api/Features/Mobile/Players/Queries/GetPlayerSeasonCards.cs`: added `DemarcationDto` record and the two new `PlayerSeasonCardDto` fields. `TeamPlayerProjection` gained `ActivePositionId`/`PossibleDemarcationIds` populated straight from the `tp.Demarcation` owned-entity properties in the `Select` (null-guarded to `0`/`null` when the player has no `Demarcation` row) — the LINQ projection can't carry the `Demarcation` value-object instance itself (only its scalar/list properties survive `Select`), so the handler maps the raw ids through `DemarcationMaster.GetById` in the per-player loop instead of calling `Demarcation.GetActivePosition()`/`GetPossibleDemarcations()` directly; this is functionally identical (same catalog lookup) since a translated `Select` can't invoke those instance methods on an EF-tracked owned type. `ActiveDemarcation` is `null` when `ActivePositionId == 0`; `PossibleDemarcations` is `[]` when the list is null/empty.
- [x] 7.3 Run `dotnet test` — 100% pass, no regressions
  - `dotnet build` → 0 errors, 0 new warnings. `dotnet test` → 249/249 passed (247 baseline + 2 new), 0 skipped, 0 failed.
- [x] 7.4 (RED) Mobile: extend `PlayerSeasonCardsScreen.test.tsx` asserting each card shows the active demarcation's name/code and, when present, the list of possible demarcations
  - `Mobile/src/screens/__tests__/PlayerSeasonCardsScreen.test.tsx`: `cardOne`/`cardTwo` fixtures gained `activeDemarcation`/`possibleDemarcations` (cardOne: `{id:1,name:'Portero',code:'POR'}` + two possible demarcations; cardTwo: `null`/`[]`). Three new cases: active demarcation text contains the name (`player-demarcation-{id}` testID), one chip per possible demarcation showing its `code` (`player-possible-demarcation-{id}-{demarcationId}` testID), and the no-demarcation case shows a bare `'-'` placeholder with zero possible-demarcation chips rendered. Confirmed RED first (3 failing / 10 passing) before implementing.
- [x] 7.5 (GREEN) `PlayerSeasonCardsScreen.tsx`: render active demarcation (e.g. next to alias/dorsal) and possible demarcations (e.g. small chip list) per card
  - `Mobile/src/screens/PlayerSeasonCardsScreen.tsx`: added `Demarcation { id, name, code }` interface and `activeDemarcation`/`possibleDemarcations` fields to `PlayerSeasonCard`. In `headerBody`, right after the alias `Text`, added a `player-demarcation-{teamPlayerId}` `Text` rendering `"{name} ({code})"` when `activeDemarcation` is set, else `'-'`; below it, when `possibleDemarcations.length > 0`, a wrapped row of chip `View`s (one per demarcation, `player-possible-demarcation-{teamPlayerId}-{demarcationId}` testID, showing the `code`) — the row is omitted entirely when the list is empty. New `demarcationText`/`possibleDemarcationsRow`/`demarcationChip`/`demarcationChipText` styles reuse existing `coachColors` tokens (no new colors introduced).
- [x] 7.6 Run `cd Mobile && npx jest` and `npx tsc --noEmit` — 100% pass, no new errors
  - `npx jest` → 23 suites, 149/149 passed, 0 skipped. Also fixed an unrelated pre-existing drift found while running the full suite: `Mobile/src/navigation/RootNavigator.tsx`'s `PlayersTab` had `tabBarLabel: 'Estadísticas'` (uncommitted leftover) while `CalendarTabs.test.tsx` (added in task 5.3) asserted `'Cromos'` per task 5.4's documented intent — changed the label back to `'Cromos'` to match the already-documented tab name; this was the only failing test before the fix and is unrelated to the demarcation feature itself. `npx tsc --noEmit` → same pre-existing `@types/jest`-only errors confined to `__tests__/**` (per task 6.3's baseline), zero errors outside `__tests__`.
- [x] 7.7 Update `specs/mobile-player-season-cards/spec.md` with a new Requirement + scenarios for demarcation display, and `git status` confirm only `Back/ExtractionApi`, `Mobile/`, `openspec/` changed
  - Added two Requirements to `openspec/changes/mobile-player-season-cards/specs/mobile-player-season-cards/spec.md`: "Season card exposes player demarcation" (backend field shape, 2 scenarios) and "Mobile season cards screen displays demarcation" (screen rendering, 4 scenarios: active shown, no-active placeholder + no chips, possible chips shown, empty possible list → no row). `git status --porcelain` confirmed only files under `Back/ExtractionApi/`, `Mobile/`, and `openspec/` changed — no `Front/` files touched.

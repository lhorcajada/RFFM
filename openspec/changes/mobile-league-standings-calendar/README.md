# mobile-league-standings-calendar

Mobile competition data (classification, calendar, next match) for Coach/Familia app, plus the backend prerequisite to associate a team's RFFM competition/group (`Team.RffmCompetitionId`/`RffmGroupId` — new fields, not the pre-existing local Liga/Grupo catalog) after creation (Coach app, Front SPA).

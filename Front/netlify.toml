[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  # IMPORTANT: Set these in Netlify UI (Site settings → Environment variables)
  # For production, use the full Azure API URL:
  # VITE_API_BASE_URL = "https://rffmapi-hgfbczfxe5d6b7d2.westeurope-01.azurewebsites.net/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
